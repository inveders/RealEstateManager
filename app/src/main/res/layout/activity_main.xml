<?xml version="1.0" encoding="utf-8"?><!-- 1 | MainActivity RootView -->
<RelativeLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main_drawer_layout"
    android:layout_width="match_parent"    
    android:fitsSystemWindows="true"
    android:orientation="vertical"
    tools:context="com.inved.realestatemanager.controller.MainActivity"
    tools:openDrawer="start" android:layout_height="match_parent">

    <!-- Include Toolbar -->
    <FrameLayout
        android:id="@+id/activity_main_frame_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"

        android:layout_below="@+id/activity_main_toolbar" />

    <!-- FrameLayout for our fragments -->
    <include layout="@layout/toolbar" />

    <!-- Viewpager for our fragments -->
    <!--     <androidx.viewpager.widget.ViewPager
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             android:id="@+id/viewpager_fragment"
             android:layout_below="@+id/activity_main_toolbar"/> -->

</RelativeLayout>

